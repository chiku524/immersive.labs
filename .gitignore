node_modules
dist
.env

# Cursor IDE local state (hooks, continual-learning indices with machine-local paths)
.cursor/
# Ephemeral tunnel credential material (rotate-tunnel / local ops)
scripts/studio-cloudflare-tunnel/.tunnel*.tmp*
apps/studio-worker/.venv/
**/.wrangler/
**/.dev.vars
*.local
.DS_Store
*.tsbuildinfo

.vercel
.env*.local

# Large local Fab drop zips (copy via scripts/sync-fab-plugin-zips-to-web.ps1)
apps/web/public/plugin-packages/**/*.zip
# Optional local source sample project zips (package-fab-product-zips.ps1)
apps/web/public/fab-samples/**/*.zip
